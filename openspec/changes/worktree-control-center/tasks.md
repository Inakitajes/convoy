## 1. Git Discovery and Checkout Locality

- [ ] 1.1 Establish disposable multi-worktree fixtures and a capability-to-implementation checklist covering all 14 deltas, including the shared `worktrees`/`control` board and artifact-focused `specs` entry defined in design D4. Verification: inspect the checklist for every delta and the same checkout-scoped routing contract without changing canonical specs in this step.
- [ ] 1.2 Implement one `git worktree list --porcelain -z` inventory parser with Git-resolved common and worktree administrative directories, canonical checkout paths, branch/detached state, and HEAD; distinguish bare metadata from executable checkouts. Verification: parser/integration tests include main, external, detached, locked, inaccessible/prunable entries, unusual paths, and no OpenSpec files.
- [ ] 1.3 Introduce observed checkout targets and shared fresh-target validation without minted identifiers, process-wide directory changes, or branch-template reconstruction; require agreeing selectors and explicit reselection when continuity is unverifiable. Verification: tests cover external move/rename, path reuse, repository replacement, branch/HEAD changes, and independent operations in two checkouts.
- [ ] 1.4 Replace global change deduplication and artifact source overlays with checkout-local active/archive/canonical readers keyed by checkout and local path; preserve husks, unreadable facts, and real dated archive destinations. Verification: fixtures with differing same-ID copies, inherited changes, missing proposals, external archive/deletion, and stray files produce independent titles, artifacts, and known/unknown task counts.
- [ ] 1.5 Add typed, timestamped independent local observations for dirt, selected-base divergence, upstream divergence, ancestry, tree equality, and execution activity; collect inventory first and detail lazily or with bounded concurrency. Verification: tests distinguish unknown from clean/absent, no upstream from zero divergence, related history from base containment, and equal content from historical integration; listing performs no fetch or domain writes.
- [ ] 1.6 Define explicit ordered checkout-local change selection and manual/no-change inputs shared by reviewed actions, without implicit singleton acceptance or inherited-file selection. Verification: selection tests preserve B-then-A order, reject stale/missing selected inputs, require singleton acceptance, and keep archive selection separate from whole-branch Git scope.

## 2. Shared Guards and Recovery Infrastructure

- [ ] 2.1 Extract generic repository storage, managed-writer claims, and mutation coordination from feature modules before replacing their consumers; keep live execution claims independent of UI attachment and checkout ownership. Verification: existing coordination tests plus concurrent-checkout tests show same-checkout conflicts, unrelated-checkout independence, and no claim release merely on client exit.
- [ ] 2.2 Add shared operation-specific inspect/review/execute guards with explicit source/base/destination facts, pinned OIDs where required, actionable disabled reasons, and effect-time revalidation; avoid a general workflow engine or lifecycle assessment. Verification: direct-handler tests reject stale targets, unknown status, detached-only incompatibilities, and conflicting managed writers while allowing independent safe actions.
- [ ] 2.3 Implement bounded versioned atomic unresolved-operation journals under `<git-common-dir>/convoy/operations/<operation-id>/` with intent-before-effect, verified acknowledgements, and protective temporary refs. Verification: storage fault tests cover interrupted writes, unreadable/version-invalid records, checkout removal survival, and separation from durable run records and compaction refs.
- [ ] 2.4 Implement recovery dispatch and safe resolution/cancellation that inspect actual effects before ordinary fresh-operation preflight, preserve uncertain candidates and evidence, and release only resolved operation journals/refs. Verification: recovery tests demonstrate no blind replay, no retained success ledger, no destruction of recoverable work, and no deletion of independent run evidence.
- [ ] 2.5 Add a narrow legacy unresolved-operation inspector and scoped conflict guard before enabling new mutations; preserve original evidence, require explicit current-state reconciliation, and stop incompatible replay with manual guidance rather than importing receipts or associations. Verification: legacy half-applied-close fixtures block conflicting targets/refs, keep unrelated checkouts usable, and remain byte-identical until explicit reconciliation.
- [ ] 2.6 Extract bounded, exactly scoped hosting observations with known/unknown/ambiguous states and an in-memory 30-second cache keyed by hosting repository, head repository/branch, and base; support timeout and manual refresh. Verification: mocked fork, reused-branch, old merged-head, authentication-failure, and multiple-match tests prove failed lookup is not absence, PR state grants no cleanup authority, and no persistent PR cache is written.

## 3. Home, Readers, and Worktree Creation

- [ ] 3.1 Replace Home's feature navigation with the shared Worktrees inventory, folder/branch/path identification, complete version and project masthead, New worktree, and auxiliary pipelines/specs/history/configuration views; retain lean chrome without a dedicated footer. Verification: wide/narrow terminal render tests cover main-only, spec-less, missing-path, and empty inventory states without Spaces branding, lifecycle summaries, or title-whitespace restrictions.
- [ ] 3.2 Wire checkout-local sections and shared observation previews into the specs browser while retaining grouped artifact tabs, full-width canonical reading, scrolling, fullscreen, copy, and discoverable reader action menus. Verification: reader tests cover same-ID copies, local archives, unreadable placeholders, compact layouts, and `c` remaining copy rather than mutation.
- [ ] 3.3 Implement verified navigation restoration and optional non-authoritative session hints; refresh local artifacts and observations together after return or explicit refresh without silently retargeting stale selection. Verification: navigation tests cover cancellation, external move/archive/removal, reused paths, absent hints, failed refresh, and no agent startup or tombstone creation from restoration.
- [ ] 3.4 Implement reviewed New worktree naming/allocation using bounded model suggestions and editable deterministic conventional fallbacks, documented/configured/default location precedence, explicit existing-branch choice, and independent versus derived base disclosure. Verification: allocation/review tests cover custom bases, spaces in paths, naming failures, collisions, unavailable location fallback, and cancellation with no branch, checkout, session, commit, or PR effects.
- [ ] 3.5 Execute accepted creation through shared guards and unresolved-operation recovery, rechecking destination occupancy without silently reallocating; select verified successful results from Git and leave authoring as a separate action. Verification: fault tests before/after branch creation and worktree addition reconcile lost acknowledgements without duplicates, overwrites, authored-file deletion, or rollback of creation after later session startup failure.

## 4. Launches, Conversations, and Run History

- [ ] 4.1 Rework worktree-scoped launch preparation to resolve configuration, pipelines, prompt history, attachments, selected changes, canonical bundle, and dirt from the execution checkout from the start; keep Apply/Continue on that checkout and preserve explicit dirty-tree consent and standalone isolation warnings. Verification: launcher tests use conflicting main/target configuration, dirty main versus dirty target, cancelled review, main execution, and deliberately requested nested isolation.
- [ ] 4.2 Remove the runner's launch-checkout artifact fallback and branch-to-change inference; revalidate the accepted target, ordered selected input snapshots, and canonical bundle before execution, including explicit headless change/manual mode. Verification: runner tests reject changed/missing inputs even when same-ID artifacts exist elsewhere, reject omitted headless mode, and run explicit spec-less/manual plans without inventing inputs.
- [ ] 4.3 Persist immutable cleanup-surviving run-start provenance and accepted input snapshots/content references without FeaturePlanLink authority; keep old runs readable and present current target observations separately. Verification: history tests retain original paths, OIDs, ordered changes, canonical inputs, reports, and unverified legacy links after branch rename, checkout move/removal, and path reuse, without granting resume or rewrite authority.
- [ ] 4.4 Update deterministic run-title precedence to the first titled explicitly selected local proposal, then humanized actual branch, then prompt, persisting once without a model call. Verification: title tests cover reviewed ordering, readable untitled proposals, missing selected artifacts, manual mode with an unselected matching change, human whitespace, and stable legacy titles.
- [ ] 4.5 Move exact harness-qualified authoring session navigation onto validated checkout locators and optional hints, with explicit session selection when continuity is absent; connect conversation, propose/revise, and Iterate without association writes. Verification: harness tests reject unrelated recent sessions and reused paths, validate moved destinations, distinguish phase sessions, preserve selected local planning context and permissions, and keep conversation usable when project authoring commands are unavailable.
- [ ] 4.6 Preserve foreground terminal recovery, explicit external presentation, service independence, and writer liveness through conversation/run navigation. Verification: conversation and terminal tests cover startup failure, non-zero exit, interrupt, resize, pane-only startup, detached active agents, explicit service-stop decisions, and conflicting writers on resume.
- [ ] 4.7 Reconnect automatic finalization to observed targets and extracted coordination while retaining run-bounded provenance, published-commit checks, bounded signing/hooks, semantic intermediate messages and exactly-one `Convoy-Run` trailer rules. Verification: compaction/message regression tests preserve durable backups, phase endpoints and journals, reject published or unverifiable replacement intervals, retain recoverable failure outcomes, and never publish automatically or create an empty trailer-only commit.

## 5. Independent Git and Publication Operations

- [ ] 5.1 Implement explicit-remote Fetch as a guarded operation separate from observation and Sync. Verification: disposable-remote tests show only requested remote refs update, with no checkout merge, implicit fetch during listing, archive, push, or base movement.
- [ ] 5.2 Implement Sync as a merge of the reviewed pinned base into a clean attached source, supporting custom local/remote bases and preserving conflict state for explicit abort/resume. Verification: tests cover base containment versus common ancestry, unrelated histories, changed reviewed refs, unknown/dirty status, conflict recovery, and absence of implicit fetch, stash, rebase, or base-checkout mutation.
- [ ] 5.3 Extract standalone Push from the run/feature publication wrapper, confirming explicit source OID and remote/ref and using a non-force pinned refspec while disclosing excluded uncommitted files. Verification: remote tests work without runs/specs/`gh`, require a choice for ambiguous upstreams, reject stale review and remote rejection without force fallback, and prove later local commits cannot join the accepted push silently.
- [ ] 5.4 Implement bounded semantic PR composition from the complete pinned source/base range with explicitly selected local proposals and optional relevant run evidence, conventional word-bounded titles, honest Why/What/How-tested fallback, and operator overrides. Verification: composition tests include no run/proposal/model, missing selected sources, later untested commits, forks/custom bases, whitespace, timeout, and explicit headless text/generated-text acceptance without an editor.
- [ ] 5.5 Implement reviewed PR creation/reuse and publication reconciliation, freezing accepted text and source inputs before effects, separately authorizing any required Push, and re-querying exact scope before retry. Verification: fault tests cover push-before-response, PR-created-before-response, lookup failures, ambiguous matches, changed source/base, and edited proposals during unchanged-scope retries without duplicate PRs, stale text reuse across changed scope, or retained success journals.
- [ ] 5.6 Connect independent Push/Create PR to run dashboards using the same current-target guards, with no run-compaction-success gate and no completion-time publication. Verification: dashboard/headless tests distinguish safely failed compaction from unresolved compaction, block the latter at every publication entry point, retain Git Push without `gh`, and require fresh worktree review for unverified historical targets without altering run provenance.

## 6. Selected OpenSpec Archive Operations

- [ ] 6.1 Add archive inspection/review for explicit ordered local active changes, known complete tasks, readable artifacts, and clean tracked/untracked state; query supported installed OpenSpec behavior before exposing any warned incomplete-task override. Verification: tests reject unknown/incomplete tasks by default, unsupported overrides, missing sources, and automatic inclusion of inherited/unselected changes while permitting an explicitly empty close archive set.
- [ ] 6.2 Execute the supported OpenSpec archive workflow in the selected checkout and inspect its actual destination and composed canonical output, using OpenSpec validation rather than structural names as semantic proof. Verification: archive fixtures cover dated destinations, unreadable output, overlapping deltas in reviewed order, ambiguous composition, and external deletion that must not count as pending-operation success.
- [ ] 6.3 Commit only verified archive output under normal operator identity, signing, hooks, and secret protections, retaining selected artifact/message context only while unresolved. Verification: tests preserve unrelated files, stop before commit on unexpected output/dirt or failed validation/hooks, and prove only the explicitly reviewed archive batch is committed.
- [ ] 6.4 Reconcile archive-before-commit and commit-before-acknowledgement interruptions before fresh cleanliness checks, checking actual files and recorded effects instead of replaying OpenSpec blindly. Verification: fault-injection tests resume verified output exactly once, stop on unexplained differences, preserve original evidence, and release resolved journals without touching run recovery data.

## 7. Squash, Close, Removal, and Fault Recovery

- [ ] 7.1 Build whole-branch squash review and a private detached candidate checkout with the pinned local base as sole parent; require base containment and clean validated source/base, retain source history, and report equal trees without an empty commit. Verification: candidate tests cover multiple authors/runs and unselected changes, unsynced/custom bases, signing/hooks/secrets, parent/tree verification, and no historical-integration or deletion claim from equality.
- [ ] 7.2 Implement guarded fast-forward candidate integration into the intended clean base checkout, revalidating source/base freshness rather than moving a checked-out ref and reconstructing files afterward. Verification: fault tests around candidate preparation, hooks, base advancement/materialization, and lost acknowledgement verify recorded preparation before recognizing an existing landing, preserve operator work, and never duplicate a squash.
- [ ] 7.3 Preserve close's bounded semantic conventional-message composition, selected-capability scope rules, selected-change body context, reviewed PR reference, verbatim `--message`, and inline Accept/Edit/Cancel. Verification: message/UI tests cover zero/one/multiple capabilities, spec-less branches, unavailable writer, operator removal of PR references, save-without-accept, cancellation, responsive composition, and terminal restoration.
- [ ] 7.4 Implement standalone guarded worktree removal with default branch retention, checking registration continuity, tracked/untracked/valuable ignored files, relevant submodules, locks, and active writers; refuse main/current-process checkout and expose deferred guarded commands without force shortcuts. Verification: removal tests preserve unsafe or unreadable content, retain branches, reject stale targets, and remove only the accepted safe linked checkout without completion records.
- [ ] 7.5 Implement separate local branch deletion with current registration checks, exact expected-tip revalidation, normal Git safety for preserved history, and explicitly destructive consent naming the branch/tip for unique-history loss. Verification: deletion tests reject checked-out or changed branches, refuse consent inferred from PR badges/receipts/tree equality, preserve branches when consent is absent, and never delete a remote branch.
- [ ] 7.6 Compose close from the same reviewed sync/archive/squash operations with complete initial preflight, explicit ordered archive selection, whole-branch scope disclosure, and separately accepted optional push/removal/deletion; preserve truthful checklist/headless progress. Verification: composite tests reject incomplete selected tasks before sync, ignore unselected task completeness, allow zero archives, name every source/base/follow-up, preserve retained worktrees, and perform no hosted merge/closure or remote deletion.
- [ ] 7.7 Complete composite and cleanup recovery for removed-before-ack, branch-deleted-before-ack, follow-up failures, and journal/ref cleanup interruption, reconciling reality before missing-target preflight. Verification: fault tests preserve remaining requested steps outside the removed checkout, avoid recreation or repeated deletion, retain uncertain evidence, and end successful close journals even when the checkout is kept so later cleanup requires fresh review.
- [ ] 7.8 Wire all implemented operations into shared Home/browser menus and the reviewed `convoy worktrees` CLI surface, including `new`, `fetch`, `sync`, `push`, `pr`, `archive`, `run`, `squash`, `close`, `remove`, `delete-branch`, and `recover`, with `convoy specs` using the same inventory and checkout-local reader. Verification: command/help and TUI handler tests cover explicit targets, identical disabled reasons, handler-time revalidation, close confirmation/cancellation, plain non-TTY listings, and no implicit launch-checkout fallback.

## 8. Compatibility and Final Legacy Removal

- [ ] 8.1 Retain standalone `convoy spin` as explicit proposal transfer using generic allocation and operation recovery, preserving change/prefix selection, deterministic naming, untracked-file transfer, pruning boundaries, opt-in wrapper installation, and truthful external `/move` output. Verification: transfer fault tests preserve moved/remaining files without overwrites or duplicate transfers, create no feature records, and show browsing/New worktree neither transfer proposals nor rewrite the global wrapper.
- [ ] 8.2 Route `convoy control` and `convoy close` to the shared board/composite, accepting legacy branch selectors only when uniquely resolved; retire `convoy feature` subcommands, feature-ID flags, and `convoy finish` with actionable non-zero diagnostics. Verification: compatibility/help tests reject old forms before Git/run effects or prompt parsing and prove no hidden finish, adoption, binding, or fabricated-association execution path remains.
- [ ] 8.3 Provide optional explicitly requested legacy-data cleanup with a preview listing only retired files/refs and proof that no unresolved legacy or new operation depends on them; otherwise leave legacy state inert and byte-identical. Verification: cleanup tests reject conflicting/unverifiable evidence, require operator consent, never remove the whole common Convoy directory, and preserve worktrees, branches, specs, sessions, run history, backups, and finalization refs/journals without automatic destructive migration.
- [ ] 8.4 After replacement consumers and recovery regression tests pass, delete obsolete feature registry readers/writers, associations, contract sets, FeaturePlanLink gates, adoption/rebinding, lifecycle assessment, receipt creation, and feature-specific UI/CLI implementations rather than renaming the registry; retain only bounded legacy historical decoding and unresolved-operation inspection where required. Verification: import/symbol and call-site audits find no remaining runtime dependency on the retired domain and extracted writer, storage, session, and run-recovery suites still pass.
- [ ] 8.5 Audit runtime persistence and visible vocabulary after deletion across browsing, creation, launch, conversation, publication, archive, close, spin, recovery, and cleanup. Verification: filesystem/ref snapshot tests assert no new feature/receipt/ownership writes, no worktree registry/tombstones/success ledger, resolved temporary data removal, untouched durable run compaction evidence, and Worktrees rather than Spaces/Features branding without rejecting valid human whitespace.

## 9. Acceptance and Documentation

- [ ] 9.1 Complete the cross-capability acceptance matrix in disposable repositories with bounded hosting mocks, filling remaining external-race, same-ID locality, fork/base, writer-liveness, ignored/submodule-content, and before/after-effect fault cases. Verification: focused `bun test` suites demonstrate no lost operator data, unintended replay, cross-checkout reads/effects, persistent lifecycle authority, or regression in conversation/terminal and compaction recovery.
- [ ] 9.2 Update user documentation and help examples for explicit worktree/change selection, independent actions, semantic PR review, optional close/cleanup, legacy command retirement and spin compatibility, conservative recovery, and rollback limitations. Verification: documentation review and CLI example tests agree with final routing, explain external-process race limits and unknown historical squash coverage, and contain no automatic migration, force-removal, or receipt-authorized cleanup recipes.
- [ ] 9.3 Run final implementation acceptance checks and review their results before marking implementation complete. Verification: `bun run typecheck`, full `bun test`, and `openspec validate worktree-control-center --type change --strict --no-interactive` all pass; inspect the final diff and persistence audit for unintended artifacts or weakened run/writer safety.
- [ ] 9.4 Document the post-implementation spec-sync/archive handoff, listing canonical Purpose paragraphs that still describe retired feature behavior and the requirement replacements to preserve. Verification: the handoff checklist covers affected capability paths and states that synchronization/archive requires its separate explicit workflow; implementation completion does not depend on archiving itself, and this task does not mutate canonical specs.
